```
usage: pcdl_get_substrate_list [-h] [-v VERBOSE] [path]

this function is returns all chemical species names, modeled in the
microenvironment, ordered by subsrate ID.

positional arguments:
  path                  path to the PhysiCell output directory or a
                        outputnnnnnnnn.xml file. default is . .

options:
  -h, --help            show this help message and exit
  -v VERBOSE, --verbose VERBOSE
                        setting verbose to True for more text output, while
                        processing. default is False.

homepage: https://github.com/elmbeech/physicelldataloader
```
